// Configuración por defecto de Playwright para RH Negativo.
// Permite ejecutar directamente:
// npx playwright test --project=chromium --workers=1 --reporter=list
module.exports = require('./playwright.rh.config.js');
